# AIKnowledgePlatform

Open-source knowledge management platform built with .NET 10 Blazor WebApp.

Quick start (development):

```bash
dotnet build                              # Build all projects
dotnet run --project AIKnowledge.Web.csproj  # Run web app (https://localhost:5001)
```

See `CLAUDE.md` for architecture and conventions.
